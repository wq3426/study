#配置servlet容器
#server.port=8080 端口号
#server.session-timeout=50 session有效时间，以秒为单位
#server.context-path=/mvc  配置项目访问路径，默认为 /

#配置tomcat
#server.tomcat.uri-encoding=UTF-8 编码，默认为UTF-8
#server.tomcat.compression=off 是否开启压缩，默认为off